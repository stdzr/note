# GPIO的工作模式  
Gpio口读入模拟信号后可以经过一个节点有上拉电阻和下拉电阻的设置，设置上拉电阻，保持高电平输入，不操作时，设置下拉电阻保持低电平输入，两个都不设置浮空输入，之后会经过施密特触发器信号直接导出为模拟输入，经过施密特触发器转化为数字信号存储到相应的寄存器，也可以复用功能输入，是数字信号进入片上外设，之后将信号转入输出设置，给定两个信号经过pMOS和nMOS，由两个信号决定输出方式是推挽输出还是开漏输出，推挽输出结果为输出高低电平，开漏输出为低电平和高阻态，在io口接上拉电阻可将高阻态转为高电平，由信号来源于片上外设还是相关寄存器来判断是否为复用输出。  
# LED
LED原理是发光二极管，信号为1时发光，用16进制控制一组8个LED
# 延时函数
void Delay(unsigned int xms)
{
	unsigned char i, j;
	while(xms--)
	{
		i = 2;
		j = 239;
		do
		{
			while (--j);
		} while (--i);
	}
}
# 独立按键
接P3的0,1,2,3口和GND,用if语句判断

